---
apiVersion: v1
kind: ReplicationController
metadata:
  name: demo-app
  labels:
    k8s-app: demo-app
spec:
  replicas: 1
  selector:
    k8s-app: demo-app
  template:
    metadata:
      labels:
        k8s-app: demo-app
    spec:
      containers:
        - image: tazjin/log-courier:latest
          name: log-courier
          volumeMounts:
            - name: log-storage
              mountPath: /var/log/nginx
        - image: tazjin/nginx
          name: nginx
          ports:
            - containerPort: 80
          volumeMounts:
            - name: www-storage
              mountPath: /usr/share/nginx/html
            - name: log-storage
              mountPath: /var/log/nginx
      volumes:
        - name: www-storage
          gitRepo:
            repository: https://github.com/tazjin/k8s-elk-demo.git
            revision: 659e65edb1800d155c75c43c11c97da3f6d6206d
        - name: log-storage
          emptyDir:
            medium: ''
